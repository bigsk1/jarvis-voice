{
  "mcpServers": {
    "fetch": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--network",
        "host",
        "mcp/fetch",
        "--ignore-robots-txt"
      ],
      "description": "Fetch and extract content from URLs as markdown. Only use when exact content is known. Does NOT check robots.txt.",
      "enabled": true
    },
    "brave_search": {
      "command": "docker",
      "args": [
        "run",
        "-e",
        "BRAVE_API_KEY",
        "-e",
        "BRAVE_MCP_DISABLED_TOOLS",
        "-i",
        "--rm",
        "--network",
        "host",
        "mcp/brave-search"
      ],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}",
        "BRAVE_MCP_DISABLED_TOOLS": "brave_llm_context"
      },
      "description": "Web search tool. Search the web for news, local search, images and videos using Brave Search. Use this when native search is not available or when you need more info from the web",
      "enabled": true
    },
    "sequentialthinking": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "mcp/sequentialthinking"
      ],
      "description": "Sequential thinking for complex tasks, use this when you need to think step by step to solve a problem",
      "enabled": false
    },
    "playwright": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--init",
        "--network", "host",
        "-v", "/tmp/playwright-output:/output",
        "mcr.microsoft.com/playwright/mcp",
        "--headless",
        "--no-sandbox",
        "--browser", "chromium",
        "--caps", "pdf",
        "--timeout-action", "30000",
        "--timeout-navigation", "60000",
        "--viewport-size", "1280x720"
      ],
      "description": "Browser automation for JS-heavy sites: navigate, click, fill forms, screenshots, PDFs. Use when fetch fails on dynamic content.",
      "enabled": false
    }
  }
}
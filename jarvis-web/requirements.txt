# Jarvis Web UI dependencies
# Keep versions aligned with the root requirements.txt; list only this server's runtime deps.
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
simple-websocket>=1.0.0
requests==2.32.5
PyYAML==6.0.2

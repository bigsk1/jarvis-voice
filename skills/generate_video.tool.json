{
  "enabled": true,
  "name": "generate_video",
  "description": "Generate AI videos with xAI Grok, Gemini Veo, or OpenAI Sora. Create videos from text prompts or images. Use when user says 'generate a video', 'create a video', 'make a video animation', 'animate this'. PROVIDERS: xAI Grok (default, 1-15s, cheapest $0.05/s), OpenAI Sora (4-12s, audio, $0.10/s), Gemini Veo (4-8s, audio, up to 4K). Videos save to stash for use with other tools.",
  "script": "generate_video.py",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "Detailed description of the video. Be specific about subject, action, movement, style, mood. For audio: Use quotes for dialogue like 'Hello!' and describe sounds. Example: 'A cat playing with a ball, slow motion, cinematic lighting'"
      },
      "provider": {
        "type": "string",
        "enum": ["xai", "gemini", "openai"],
        "description": "Video provider: 'xai' (default, Grok - 1-15s, cheapest), 'openai' (Sora 2 - 4-12s, audio, remix), 'gemini' (Veo 3.1 - 4-8s, audio, up to 4K). If not specified, uses VIDEO_TOOL_PROVIDER from config."
      },
      "duration": {
        "type": "integer",
        "description": "Video duration in seconds. xAI: 1-15s (any). OpenAI: rounds to 4, 8, or 12s. Gemini: rounds to 4, 6, or 8s. Default: 5 seconds."
      },
      "aspect_ratio": {
        "type": "string",
        "enum": ["16:9", "4:3", "1:1", "9:16", "3:4", "3:2", "2:3"],
        "description": "Video shape. xAI supports all 7. Gemini/OpenAI only support '16:9' (widescreen) or '9:16' (vertical) - others mapped."
      },
      "resolution": {
        "type": "string",
        "enum": ["720p", "480p", "1080p", "4k"],
        "description": "Video resolution. xAI: 720p/480p. OpenAI: 720p or 1080p (pro model). Gemini: 720p-4k (1080p/4k requires 8s)."
      },
      "image_url": {
        "type": "string",
        "description": "Image source for image-to-video. Accepts: stash reference (stash://space_xxx/file_id), local file path, or URL. For uploaded images, use the stash reference from 'Image stashed at:' in the conversation."
      },
      "video_url": {
        "type": "string",
        "description": "Edit an existing video's visual content/style ONLY. xAI editing CANNOT change duration, aspect ratio, or resolution — only what happens visually. If user wants different duration/size, regenerate from source image using image_url instead. xAI: MUST be a public http(s) URL, stash refs will NOT work. OpenAI: video ID to remix (starts with 'video_'). Provider URLs expire after ~4 hours."
      },
      "negative_prompt": {
        "type": "string",
        "description": "GEMINI ONLY: What to avoid in the video. Example: 'cartoon, low quality, blurry'"
      },
      "save": {
        "type": "boolean",
        "description": "Save to stash for use with other tools (default: true). Video is downloaded and stored locally."
      }
    },
    "required": ["prompt"]
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": true,
    "filesystem": true,
    "auto_approve": true
  }
}

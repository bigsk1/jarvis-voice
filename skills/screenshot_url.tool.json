{
  "enabled": true,
  "name": "screenshot_url",
  "description": "Take a screenshot of a webpage and optionally analyze it with vision. Use for 'screenshot this page', 'what does this website look like', or 'capture this webpage'. Use when the need is visual: charts, layout, or page state. Returns a screenshot path and optional AI analysis. For page text, use crawl_url. For following links in a browser, use browser_use.",
  "script": "screenshot_url.py",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL of the webpage to screenshot"
      },
      "analyze": {
        "type": "boolean",
        "description": "If true, analyze the screenshot with vision AI and describe what's visible",
        "default": false
      },
      "question": {
        "type": "string",
        "description": "Specific question to ask about the screenshot (only used if analyze=true)",
        "default": "Describe what you see on this webpage"
      },
      "wait": {
        "type": "number",
        "description": "Seconds to wait before taking screenshot (for JS to load)",
        "default": 2
      },
      "save_path": {
        "type": "string",
        "description": "Optional: specific path to save screenshot (default: temp directory)"
      }
    },
    "required": ["url"]
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": true,
    "filesystem": true,
    "auto_approve": true
  },
  "availability": {
    "all_of_env": ["CRAWL4AI_URL"],
    "setup_hint": "Set CRAWL4AI_URL in the active mode env file. CRAWL4AI_USER, CRAWL4AI_PASS, and CRAWL4AI_API_KEY are optional."
  }
}


{
  "enabled": true,
  "name": "bookmark_search",
  "description": "Parse and search Firefox bookmark export files (Netscape HTML format). Supports keyword and quoted phrase search, plus filters by tags, folder path fragments, and domains. Actions: search, list_tags, list_folders, stats.",
  "complementary_tools": [
    "canvas",
    "stash",
    "manage_intel"
  ],
  "script": "bookmark_search.py",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "search",
          "list_tags",
          "list_folders",
          "stats"
        ],
        "description": "Operation to run. Default is 'search'."
      },
      "file_path": {
        "type": "string",
        "description": "Bookmark HTML file path. Default: data/bookmarks.html"
      },
      "query": {
        "type": "string",
        "description": "Search query. Supports quoted phrases, e.g. \"chili youtube\" or \"food license\" oregon. Unquoted multi-word queries require every word to match (AND) by default so generic terms like youtube do not match every youtube.com bookmark. Omit query_mode to get that behavior; set query_mode to \"any\" only if you need OR matching (any single word)."
      },
      "tags": {
        "type": [
          "string",
          "array"
        ],
        "items": {
          "type": "string"
        },
        "description": "Tag filter(s), comma-separated string or array. Matches Firefox TAGS and folder-derived labels."
      },
      "folders": {
        "type": [
          "string",
          "array"
        ],
        "items": {
          "type": "string"
        },
        "description": "Folder path fragment filter(s), comma-separated string or array."
      },
      "domains": {
        "type": [
          "string",
          "array"
        ],
        "items": {
          "type": "string"
        },
        "description": "Domain filter(s), comma-separated string or array (e.g., github.com, reddit.com)."
      },
      "query_mode": {
        "type": "string",
        "enum": [
          "any",
          "all"
        ],
        "description": "How to combine search terms: omit for smart default (AND when multiple words/phrases; OR only for a single term). \"all\" = every term/phrase must match. \"any\" = match if any term matches (broad; use for deliberate OR searches)."
      },
      "sort_by": {
        "type": "string",
        "enum": [
          "relevance",
          "recent",
          "title",
          "domain"
        ],
        "description": "Sort order for search results. Default: relevance."
      },
      "include_duplicates": {
        "type": "boolean",
        "description": "Include duplicate URLs in results. Default: true."
      },
      "limit": {
        "type": "integer",
        "description": "Max results to return (1-200). Default: 20."
      },
      "offset": {
        "type": "integer",
        "description": "Pagination offset for search. Default: 0."
      }
    },
    "required": []
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": false,
    "filesystem": true,
    "auto_approve": true
  },
  "test_input": {
    "action": "search",
    "query": "\"food license\" oregon",
    "limit": 5
  },
  "expected_output_contains": [
    "ok",
    "results",
    "matched_count"
  ]
}

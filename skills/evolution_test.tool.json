{
  "enabled": false,
  "name": "evolution_test",
  "description": "A test tool for validating evolutionary algorithm implementations and genetic programming systems. Use this tool when you need to verify mutation operations, test fitness functions, validate selection mechanisms, or debug population dynamics in evolutionary computing scenarios.",
  "script": "evolution_test.py",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "The action to perform",
        "enum": [
          "echo",
          "status",
          "fail"
        ]
      },
      "message": {
        "type": "string",
        "description": "Message for echo action"
      }
    },
    "required": []
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": false,
    "filesystem": false,
    "auto_approve": true
  }
}
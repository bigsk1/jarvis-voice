{
  "enabled": true,
  "name": "canvas",
  "description": "Save and read rich content from Jarvis Canvas - a visual viewer for research results, code snippets, comparisons, and reference material.\n\nIMPORTANT: For research tasks, create canvas as your FINAL step AFTER gathering ALL data. If you already created a canvas but gathered more data, use action='update' with page_id to add the new information.\n\nUse when:\n- User asks to research something and results are complex\n- User asks to 'save to canvas' or 'put in viewer'\n- Content has tables, code, or detailed formatting\n- User asks 'what's on my canvas?' or 'read my canvas page about X'\n- User wants to summarize/read a specific canvas page\n\nFinding pages:\n- 'summarize the 69x Opportunity page' → action=read, search='69x Opportunity'\n- 'read my notes from yesterday' → action=read, search='Notes/2026-01'\n- 'what research do I have?' → action=read, search='Workflows/Research'\n- 'show all canvas pages' → action=list\n\nActions: create (new page), update (modify existing), delete (remove), list (show all), open (get URL), read (find and get page content by search term or page_id).",
  "script": "canvas.py",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["create", "update", "delete", "list", "open", "read"],
        "description": "Action to perform. create=new page, update=modify existing, delete=remove, list=show all, open=get URL, read=get page content"
      },
      "title": {
        "type": "string",
        "description": "Page title (required for create, optional for update)"
      },
      "content": {
        "type": "string",
        "description": "Markdown content for the page. REQUIRED for create/update. For update: pass the FULL new content (read existing first, add new sections, then pass combined). Supports headers, lists, tables, code blocks. If you include sources/links, URLs must be complete and resolvable. NEVER use shortened/truncated links like 'https://...'."
      },
      "tags": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Tags for categorization. Examples: research, code, reference, movies, servers"
      },
      "source_query": {
        "type": "string",
        "description": "The original user query that led to this content (for reference)"
      },
      "page_id": {
        "type": "string",
        "description": "Page ID for update/delete/read actions. Format: page_YYYYMMDD_HHMMSS"
      },
      "search": {
        "type": "string",
        "description": "Search term to find pages by title or content (for read action). Searches folder paths too. Examples: '69x Opportunity', 'Workflows/Deep Dive', 'Notes/', 'bitcoin price'."
      },
      "pinned": {
        "type": "boolean",
        "description": "Pin page to top of list"
      },
      "limit": {
        "type": "integer",
        "description": "Max pages to return for list action (default 10)"
      }
    },
    "required": ["action"]
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": true,
    "filesystem": false,
    "auto_approve": true
  }
}


{
  "enabled": true,
  "name": "search_memory",
  "description": "Search all stored memories by keyword (fuzzy text matching). Same as recall but with higher default limit. Use this to see what you know about a topic or find related memories. For natural language questions like 'What food do I love?', use semantic_recall instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search term to find in stored memories"
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of results",
        "default": 10
      }
    },
    "required": [
      "query"
    ]
  },
  "permissions": {
    "filesystem": true,
    "auto_approve": true
  }
}

{
  "enabled": true,
  "name": "search_memory",
  "description": "KEYWORD/ENTITY SEARCH using FTS5 with BM25 ranking. USE THIS when user mentions specific names, entities, or technical terms they want to find. Fast exact matching with stemming support. Use for queries like: 'find info about X', 'locate Y details', 'what is Z', 'X configuration'. For conceptual questions without specific entity names, use semantic_recall instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search term to find in stored memories"
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of results",
        "default": 10
      }
    },
    "required": [
      "query"
    ]
  },
  "permissions": {
    "filesystem": true,
    "auto_approve": true
  }
}

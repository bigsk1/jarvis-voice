{
  "enabled": false,
  "name": "samantha",
  "description": "Contact Samantha, a separate AI assistant on remote cloud VPS. ONLY use when user EXPLICITLY asks to 'ask Samantha', 'have Samantha do', 'tell Samantha', or 'check with Samantha'. She is like a remote colleague - has her own tools (Discord/Telegram posting, browser automation, cron scheduling, web search) but NO access to Jarvis's local network, home systems, prices, weather, or memory. Never auto-delegate your tasks to her - user must specifically request Samantha by name.",
  "script": "samantha.py",
  "parameters": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "Task for Samantha. Include all context she needs - she cannot see your conversation history. Be specific about what response you want back."
      },
      "session": {
        "type": "string",
        "description": "Session ID for conversation context. Use 'jarvis' (default) to continue previous conversations - Samantha will remember prior context. Use unique ID like 'jarvis-research-jan26' for isolated one-off tasks that shouldn't affect main conversation history. Same session = Samantha has context, different session = fresh start.",
        "default": "jarvis"
      },
      "priority": {
        "type": "string",
        "enum": [
          "urgent",
          "normal",
          "background"
        ],
        "description": "Task priority. 'urgent' = needs immediate attention, 'normal' = standard (default), 'background' = can wait/lower priority.",
        "default": "normal"
      },
      "timeout": {
        "type": "integer",
        "description": "Timeout in seconds. Quick tasks: 30-60s. Complex research: 120s (default). Very long tasks: up to 300s.",
        "default": 240
      }
    },
    "required": [
      "message"
    ]
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": true,
    "filesystem": false,
    "auto_approve": true
  },
  "test_input": {
    "message": "Hello, this is a test from Jarvis. Please respond briefly.",
    "timeout": 30
  },
  "expected_output_contains": [
    "ok",
    "response"
  ],
  "availability": {
    "all_of_env": [
      "SAMANTHA_GATEWAY_TOKEN"
    ],
    "setup_hint": "Set SAMANTHA_GATEWAY_TOKEN in the active mode env file."
  }
}

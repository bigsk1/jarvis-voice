{
  "enabled": true,
  "name": "serpapi_ebay_product",
  "description": "Fetch one eBay product page via SerpApi (engine ebay_product) by numeric product_id from listing URLs (/itm/{id}). Returns product_summary (title, prices buy/bid, shipping to/from, quantity, images, variation counts), seller_summary, and related_products. Full JSON is optional via include_raw. Prefer no_cache=false unless you truly need bypassing SerpApi cache. Docs: https://serpapi.com/ebay-product-api",
  "complementary_tools": [
    "canvas",
    "stash",
    "serpapi_ebay_search"
  ],
  "script": "serpapi_ebay_product.py",
  "proxy_policy": "off",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "eBay item id (digits from https://www.ebay.com/itm/{product_id})."
      },
      "ebay_domain": {
        "type": "string",
        "description": "eBay site host, default ebay.com."
      },
      "locale": {
        "type": "string",
        "description": "Optional SerpApi locale for search origin."
      },
      "lang": {
        "type": "string",
        "description": "Optional language override (see SerpApi docs; US domain + locale)."
      },
      "shipping_country": {
        "type": "string",
        "description": "Optional ISO-style shipping country for shipping quotes (SerpApi shipping_country)."
      },
      "no_cache": {
        "type": "boolean",
        "description": "Bypass SerpApi cache when true. Default false."
      },
      "extra_params": {
        "type": "object",
        "description": "Additional SerpApi parameters excluding reserved keys."
      },
      "include_raw": {
        "type": "boolean",
        "description": "Include full SerpApi payload under data.raw."
      }
    },
    "required": [
      "product_id"
    ]
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": true,
    "filesystem": false,
    "auto_approve": true
  },
  "availability": {
    "all_of_env": [
      "SERP_API_KEY"
    ],
    "setup_hint": "Set SERP_API_KEY in the active mode env file."
  }
}

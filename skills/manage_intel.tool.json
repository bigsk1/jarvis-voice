{
  "enabled": true,
  "name": "manage_intel",
  "description": "SAVE TO INTEL FOLDER: Create, read, update, delete, or list files in jarvis-intel/ directory. This is the ONLY tool that can write to jarvis-intel/ - OpenCode CANNOT access this folder. Use when user says 'save to intel', 'add to my intel', 'save to my knowledge base', or 'create intel file'. Sandboxed to jarvis-intel/ only (FLAT - no subdirs). Set auto_ingest=true to add facts to memory. Actions: create, read, update, delete, list.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "create",
          "read",
          "update",
          "delete",
          "list"
        ],
        "description": "Action to perform: create (new file), read (get content), update (modify existing), delete (remove file + memories), list (show all files)"
      },
      "path": {
        "type": "string",
        "description": "Relative path within jarvis-intel/ (e.g., 'servers/coolify-issue.md' or 'network-config.txt'). Required for create/read/update/delete."
      },
      "content": {
        "type": "string",
        "description": "File content (markdown or plain text). Required for create/update actions."
      },
      "pattern": {
        "type": "string",
        "description": "File name pattern for list action (e.g., '*server*'). Defaults to '*' (all files)."
      },
      "auto_ingest": {
        "type": "boolean",
        "description": "If true, automatically run ingest_intel after create/update/delete to update memory. Recommended: true."
      }
    },
    "required": [
      "action"
    ]
  },
  "handler": "skills/manage_intel.py",
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": false,
    "filesystem": true,
    "auto_approve": false
  }
}
{
  "name": "opencode",
  "description": "Execute complex autonomous tasks using OpenCode: coding, building projects, deploying, file operations, git operations, analysis, and multi-step workflows",
  "parameters": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "description": "Detailed description of the task to execute. Be specific about what needs to be done."
      },
      "task_type": {
        "type": "string",
        "enum": ["coding", "build_website", "fix_bug", "analysis", "deploy", "general"],
        "description": "Type of task - determines workspace and permissions (optional, defaults to 'general')"
      },
      "session_id": {
        "type": "string",
        "description": "Optional: OpenCode session ID to resume previous conversation/task"
      },
      "model": {
        "type": "object",
        "description": "Optional: Override model selection",
        "properties": {
          "providerID": {
            "type": "string",
            "description": "Provider ID (e.g., 'anthropic', 'openai')"
          },
          "modelID": {
            "type": "string",
            "description": "Model ID (e.g., 'claude-sonnet-4-5-20250929')"
          }
        }
      }
    },
    "required": ["task"]
  },
  "permissions": {
    "dangerous": true,
    "filesystem": true,
    "network": true,
    "bash": true,
    "auto_approve": false
  }
}

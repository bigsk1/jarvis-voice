{
  "name": "forget",
  "description": "Delete a memory that is no longer needed, incorrect, or outdated. Use this to keep the knowledge base clean and current. First use 'recall' to find the memory ID, then forget it.",
  "parameters": {
    "type": "object",
    "properties": {
      "memory_id": {
        "type": "integer",
        "description": "The ID of the memory to delete (from recall results)"
      }
    },
    "required": ["memory_id"]
  },
  "permissions": {
    "filesystem": true,
    "auto_approve": true
  }
}


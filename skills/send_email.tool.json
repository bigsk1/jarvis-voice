{
  "enabled": true,
  "name": "send_email",
  "description": "Send an email to a contact, optionally with file attachments from stash. Use when user says 'email', 'send email to', 'email me that image', 'send email', etc. Contacts looked up by name (e.g., 'Andrew', 'Mom', 'Boss') from contacts.json. SUPPORTS ATTACHMENTS: Use 'attachment' parameter with stash ref (stash://space_xxx/file) or file path to attach images, PDFs, etc. Gracefully handles expired stash (7-day TTL) with fallback to generated_images/. IMPORTANT: Always compose email body with context - who is sending and why.",
  "script": "send_email.py",
  "parameters": {
    "type": "object",
    "properties": {
      "to": {
        "type": "string",
        "description": "Recipient name (from contacts) or email address. Examples: 'Andrew', 'mom', 'Boss', 'john@example.com'"
      },
      "subject": {
        "type": "string",
        "description": "Email subject line - make it descriptive"
      },
      "body": {
        "type": "string",
        "description": "Email body. Start with context: who sent it and why, then content."
      },
      "attachment": {
        "type": "string",
        "description": "File to attach: stash ref (stash://space_xxx/file) OR file path (/path/to/file.jpg). For generated images, use the stash_ref from generate_image output. Handles expired stash gracefully with fallback."
      },
      "attachment_path": {
        "type": "string",
        "description": "Alternative: direct file path if you have it instead of stash ref"
      },
      "image_url": {
        "type": "string",
        "description": "Optional: URL of image to embed inline (not as attachment)"
      },
      "link_url": {
        "type": "string",
        "description": "Optional: Clickable link URL"
      },
      "link_text": {
        "type": "string",
        "description": "Optional: Display text for the link"
      }
    },
    "required": ["to", "subject", "body"]
  },
  "permissions": {
    "dangerous": false,
    "bash": false,
    "network": true,
    "filesystem": true,
    "auto_approve": false
  },
  "availability": {
    "webhook_registry": ["send_email"],
    "setup_hint": "Add an enabled send_email entry with a configured URL in config/webhook_registry.json (direct URL or ${N8N_LOCAL_API_URL}/...). contacts.json is optional when using a direct email address."
  }
}

